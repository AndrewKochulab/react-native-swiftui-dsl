/**
 * Animation and transition token constants.
 */

export enum Easing {
  linear = 'linear',
  easeIn = 'easeIn',
  easeOut = 'easeOut',
  easeInOut = 'easeInOut',
  spring = 'spring',
}

export enum AnimationType {
  timing = 'timing',
  spring = 'spring',
}

export enum Transition {
  opacity = 'opacity',
  slide = 'slide',
  scale = 'scale',
  move = 'move',
}

export enum TransitionEdge {
  top = 'top',
  bottom = 'bottom',
  leading = 'leading',
  trailing = 'trailing',
}
